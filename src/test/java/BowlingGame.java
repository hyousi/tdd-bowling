import java.util.List;

/**
 * simulates a line of bowling game.
 *
 * @author t0iiz
 */
public class BowlingGame {

    /** calculates the total score.  */
    public int getScore() {
       return 0;
    }

    /** calculates the points for a specific frame. */
    public int getFrameScore(int frame) {
        return 0;
    }

    /** simulates the action of roll a ball. */
    public void roll(int num) {

    }

    /** record the status of a frame of the game. */
    private class Frame {
        private List<Integer> recordList;

        void addRecord(int record) {
            return;
        }
    }
}
